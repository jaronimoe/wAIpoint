{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Work Package",
  "type": "object",
  "required": ["name", "status", "created"],
  "properties": {
    "name": { "type": "string" },
    "description": { "type": "string" },
    "status": {
      "type": "string",
      "enum": ["planned", "in-progress", "blocked", "done"]
    },
    "priority": {
      "type": "string",
      "enum": ["low", "medium", "high"]
    },
    "blocked_by": {
      "type": "string",
      "description": "Slug of the work package in the same project that blocks this one; only set while status is blocked"
    },
    "notes": {
      "type": "string",
      "description": "Brief context — why it is blocked, dependencies, decisions"
    },
    "created": { "type": "string", "format": "date-time" },
    "updated": {
      "type": "string",
      "format": "date-time",
      "description": "Last write through the CLI"
    },
    "edited": {
      "type": "string",
      "format": "date-time",
      "description": "Last write through the dashboard"
    }
  }
}
